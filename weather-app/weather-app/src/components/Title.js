import React from "react";

const Title = () => {
    return (
        <div>
            <header>
                <h1>Weather App</h1>
                <h2>Find out the weather!</h2>
            </header>
        </div>
    );
};

export default Title;