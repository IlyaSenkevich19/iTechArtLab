const initialState = {
    city: 'Minsk',
    country: '',
}

// export default (state = initialState, action) => {
//     switch (action.type) {
//         case :
//             return 
//         case :
//             return (state, action.payload);
//         default: return state;
//     }
// }